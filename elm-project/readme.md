# Some Sample Expressions #

    A*sin(W*x)*B*cos(V*y)
    A*sin(W*x)*B*(e^cos(V*y))
    A*sin(W*x)+B*cos(V*y)
    sin(W*(x+cos(V*y)))
    sin(W*(x*cos(V*y)))
    M1*sin(A*x)*sin(B*y)*cos(C*x)*cos(D*y)
    M1*sin(A*x)+sin(B*y)+cos(C*x)+cos(D*y)
    A*sin(W*x)*B*(e^cos(V*y))
    A*sin(W*x)-B*cos(V*y)
    cos(x*y)
    sin(2*pi*cos(x+y))
    A*(sin(V*x)^2)*B*(cos(W*y)^2)
    sin(A*x+B*y)
    A*sin(V*x)+B*cos(W*x)
    cos(abs(W*x)+abs(V*y))*M*(abs(x)+abs(y))
    sin(W*cos(V*(x+y)))    
    cos(e^(sin(x^2+y^2)))
    (x^2+3*y^2)*e^(-1)*(x^2-y^2)
    abs(x)*cos(y)
    sin(A*x+B*y)*cos(C*x-D*y)
    sin(W*x)*M*e^(cos(V*y))
For debugging the grid:

    (0*x)+(0*y)+1



(01) x*y^3-y*x^3
(02) (x^2+3*y^2)*e^(-x^2-y^2)
(03) -x*y*e^(-x^2-y^2)
(04) -1/(x^2+y^2)
(05) cos(abs(x)+abs(y))
(06) cos(abs(x)+abs(y))*(abs(x)+abs(y))

Reference: https://www.physicsforums.com/threads/cool-3-d-functions-for-graphing.140087/
